#include <stdio.h>
#include <stdlib.h>

int main()
{
    int N; //Numero de cartas
    int K; //Embaralhamentos
    int final[N];

    scanf("%d", &N);

    for (int i = 0; i < N; i++)
    {
        scanf("%d", &final[i]);

    }

    return 0;
}